#include "main.h"
/**
 * _memset() - writes a function that fils memory with a constant byte
 * @n - byte
 * @s - pointer
 * @b - constant byte
 * Return: Nothing
*/
char *_memset(char *s, char b, unsigned int n)
	unsigned int n;
	int *s;
	char buffer[n] = {0x00};
	*s = buffer[n];

	const byte b[10];
	memset(buffer[n], sizeof(b));
